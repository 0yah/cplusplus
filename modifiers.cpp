#include <iostream>

using namespace std;

int main (){
    //Modifiers are used to change the meaning of the base type
    //Changes the bitwise pattern

    signed int a; //Stores only intergers
    unsigned int b; //Stores only non negative intergers
    short c; //Stores only small intergers
    long d; //Store only larger intergers
    unsigned long; //Used for large positive integers or 0
    long long; //Stores very large intergers
    unsigned long long;// Stores veruy large positive intergers
    long double; // Stores large floating point numbers
    signed char; //Stores only characters (-127 to 127)
    unsigned char; //Used go characters(0 to 255)




    return 0;
}